"""Shared helpers for X5 MMM refresh, forecasting and optimization.

This package is intentionally small. The accepted production model still lives in
notebooks/scripts until we port stable pieces deliberately. New workflows should
import shared IO, planning and reporting helpers from here only when reuse is real.
"""
