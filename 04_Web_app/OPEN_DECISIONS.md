# X5 MMM Open Decisions

Snapshot date: `2026-07-17`.

The owner has accepted a `research_pilot` stage with allocation-only output,
one external server as the likely first deployment, temporary file-backed
state with retention, and no requirement to wait for sealed OOT before product
testing. These decisions do not relabel the model package as production-ready.

This file lists only decisions that require explicit approval from the user, model owner, Marketing, Finance, IT/platform, security, or data-governance owners. Verified facts and already frozen architecture boundaries are not repeated here.

| ID | Approval owner | Decision requiring approval | Current position and evidence | Blocks |
|---|---|---|---|---|
| `OD-001` | Model owner and data owner | Define the sealed OOT period, data completeness criteria, target availability, acceptance thresholds, and sign-off procedure when a complete new period becomes available. | No valid OOT artifact is present; the package blocker is `MISSING_OR_FAILED_OOT_VALIDATION`. Research-pilot use with an explicit caveat was accepted on 2026-07-15. | Formal production-model claim; does not block local or research-pilot forecast/allocation. |
| `OD-002` | Finance, Marketing, product owner, and model owner | Approve a future commercial decision policy: turnover ROAS versus contribution margin, VAT and cost treatment, threshold values and whether any output may support launch/cancel. | Current policy is `business_hurdle_unapproved_v1`; the owner accepted `allocation_only`, so forecast, scenario comparison and budget allocation remain available. | Only business-hurdle and launch/cancel verdicts; does not block allocation. |
| `OD-003` | Marketing/media owner and product owner | Approve first-class media-plan constraints when recommendations are expected to become directly executable: inventory, contractual minima/maxima, mandatory placements, geo exclusions and lock rules. | These inputs are absent. Research-pilot output is an analytical allocation recommendation with explicit caveats. | Direct operational plan approval; does not block research optimization. |
| `OD-004` | Product owner and model owner | Tune Scenario 6 server search profiles after pilot latency measurements: interactive target, search/time budget, deeper second stage and timeout behavior. | Current bounded adaptive search is usable and makes no global-optimum claim. Product testing will provide the missing latency evidence. | Final SLA and capacity sizing; does not block pilot testing. |
| `OD-005` | IT/platform and database owners | Approve the future company runtime topology: PostgreSQL, durable queue, object storage, observability, backup and environment promotion. | For research pilot, one VM with file-backed state, one worker, retention and backup is accepted and its deployment source is implemented. The actual VM/domain still must be selected and provisioned. | Company-contour or multi-node deployment only. |
| `OD-006` | Security, IAM/IT, and product owner | Approve SSO/OIDC, MFA and corporate group mapping if the tool enters the company contour. | Research pilot has local Argon2id accounts, server-side sessions, viewer/analyst/admin permissions and append-only audit behind HTTPS/reverse-proxy perimeter controls. It exposes no model activation UI. | Company-contour authentication only; does not block the local pilot. |
| `OD-007` | Security, data governance, and IT | Approve company-data controls if sensitive or regulated files are introduced: storage location, encryption, malware scanning, DLP and audit retention. | Research-pilot runtime now has configurable terminal-resource retention; the owner described inputs as future campaign specifications rather than a corporate system of record. | Upload of data classified as sensitive/company-restricted to an external server. |
| `OD-008` | Model owner, product owner, and governance/security owners | Approve model lifecycle authority: who may promote, roll back, or manually approve restricted/extrapolative results; required evidence; four-eyes review; and audit/incident procedure. | Registry mechanisms exist, but application roles and approval ownership are not approved. Evidence: `04_Web_app/PROJECT_BRIEF.md`; `04_Web_app/CURRENT_TRUTH.md`. | Admin model-registry UI and production promotion workflow. |
| `OD-009` | Company data/security owner and engineering owner | Approve repository governance if the project becomes an official company asset. | The owner authorized source-only use of `AYUKor/MMM_platform`; CI checks sanitized/source fixtures and real data/model packages remain excluded. | Official company ownership and CI/CD policy; does not block the current research repository. |

The following are already frozen and are therefore not open decisions: reuse of the existing MMM core, no duplicated mathematics, job-level DecisionResult with `campaign_results[]`, PostgreSQL as application-state truth, artifacts outside PostgreSQL, separate preprod development and production activation, the canonical lifecycle vocabulary, and the five machine-code status domains.
